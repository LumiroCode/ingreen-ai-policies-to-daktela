Only DaktelaModule and DaktelaTabSignatureVerifier are public interfaces of this module. All other files must be hidden in directories and not accessed directly from outside this module!

Daktela identifies tickets by the `name` field in API payloads and URLs. In this codebase we call the same value `ticketId`, because it is the stable ticket identifier used by callers.
